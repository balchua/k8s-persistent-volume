apiVersion: v1
kind: PersistentVolume
metadata:
  name: my-nfs
  namespace: do-myproject
  labels:
    type: my-nfs
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    # FIXME: use the right IP
    server: 10.130.26.56
    path: "/export/files"
